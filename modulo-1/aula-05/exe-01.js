const prompt = require('prompt-sync')();
/*

let usuario = '';
let senha = '';

do {
    if (usuario === senha && usuario.length > 3){
        console.log('Erro, senha e usuário não podem ser iguais.');
    }
    usuario = prompt('Digite o nome de usuário: ');
    senha = prompt('Digite a sua senha: ');
} while (usuario === senha);
*/

